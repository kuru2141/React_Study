import React from "react";
// import { useSearchParams } from "react-router-dom";

const Home = () => {
  // // query string을 쓰고 싶은 경우
  // const [params, setParams] = useSearchParams();
  // console.log(params.get("value"));

  return <div>Home</div>;
};

export default Home;
